# kcl-lib
Semantic diff tool for KCL (Zoo's CAD language) — parses two .kcl files with Zoo's own kcl-lib and reports structural changes to parameters, sketches, and pipelines, with an optional live 3D preview. Rust → WASM core, Next.js frontend.
